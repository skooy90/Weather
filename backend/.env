# 환경 변수는 Render 대시보드에서 설정합니다.

# 서버 설정
PORT=10000
NODE_ENV=production

# 데이터베이스 설정
MONGODB_URI=mongodb+srv://skooy90:dbhana951201@skyman.r0gqkcz.mongodb.net/weather?retryWrites=true&w=majority

# CORS 설정
CORS_ORIGIN=https://kysong.store

# 보안 설정
JWT_SECRET=your_secure_jwt_secret_key

# 로깅 설정
LOG_LEVEL=info
LOG_FILE=logs/app.log 